// acmicpc number: 9372
#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

ll t, n, m;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> t;
    for (int T = 0; T < t; T++)
    {
        cin >> n >> m;
        for (int i = 0; i < m; i++)
        {
            int s, e;
            cin >> s >> e;
        }
        cout << n - 1 << "\n";
    }
}